"use client"

import React from "react";
import Navbar from "@/components/navigation/navbar";

const Header = () => {
  return (
    <header>
      <Navbar/>
    </header>
  );
};

export default Header;
